import utils
import modules
import visuals

Model:
    imports*=Import
    (
        (metadata=Metadata)
        (mods=Modules)?
        (visuals=Visuals)?
    )#
;

Metadata:
    'Metadata:'
    (
        ('title:' name=STRING)
        ('description:' description=STRING)?
        ('version:' version=STRING)?
    )#
;