import utils
import modules
import visuals
import main_components

Model:
    imports*=Import
    (
        (metadata=Metadata)
        (mods=Modules)?
        (visuals=Visuals)?
        (main=MainComponents)?
    )#
;

Metadata:
    'Metadata:'
    (
        ('title:' name=STRING)
        ('description:' description=STRING)?
        ('version:' version=STRING)?
    )#
;